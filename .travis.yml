language: java
os:
  - linux
  - osx
env:
  global:
    - QTM_BIN=./QTM
    - INFILE=articles.txt
    - OUTFILE=qtl.csv
install:
  - mvn install
  - solr/install_solr.sh
script:
  - $QTM_BIN -v
  - $QTM_BIN -V 7 $INFILE
  - |
    [[ -s "$OUTFILE" ]]

